diff --git a/ggml/src/ggml-backend-dl.cpp b/ggml/src/ggml-backend-dl.cpp
index a65cf0090..94dc4bb64 100644
--- a/ggml/src/ggml-backend-dl.cpp
+++ b/ggml/src/ggml-backend-dl.cpp
@@ -31,10 +31,17 @@ const char * dl_error() {
 
 #else
 
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+dl_handle * dl_load_library(const char * path) {
+    dl_handle * handle = dlopen(path, RTLD_NOW | RTLD_LOCAL);
+    return handle;
+}
+#else
 dl_handle * dl_load_library(const fs::path & path) {
     dl_handle * handle = dlopen(path.string().c_str(), RTLD_NOW | RTLD_LOCAL);
     return handle;
 }
+#endif
 
 void * dl_get_sym(dl_handle * handle, const char * name) {
     return dlsym(handle, name);
diff --git a/ggml/src/ggml-backend-dl.h b/ggml/src/ggml-backend-dl.h
index f74b7c948..4df5e2708 100644
--- a/ggml/src/ggml-backend-dl.h
+++ b/ggml/src/ggml-backend-dl.h
@@ -1,5 +1,7 @@
 #pragma once
 
+#include <memory>
+
 #ifdef _WIN32
 #   define WIN32_LEAN_AND_MEAN
 #   ifndef NOMINMAX
@@ -11,9 +13,13 @@
 #    include <dlfcn.h>
 #    include <unistd.h>
 #endif
-#include <filesystem>
 
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+// std::filesystem is unavailable in the NDK libc++ for API < 21.
+#else
+#    include <filesystem>
 namespace fs = std::filesystem;
+#endif
 
 #ifdef _WIN32
 
@@ -39,7 +45,11 @@ struct dl_handle_deleter {
 
 using dl_handle_ptr = std::unique_ptr<dl_handle, dl_handle_deleter>;
 
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+dl_handle * dl_load_library(const char * path);
+#else
 dl_handle * dl_load_library(const fs::path & path);
+#endif
 void * dl_get_sym(dl_handle * handle, const char * name);
 const char * dl_error();
 
diff --git a/ggml/src/ggml-backend-reg.cpp b/ggml/src/ggml-backend-reg.cpp
index 058710921..eb4c9be33 100644
--- a/ggml/src/ggml-backend-reg.cpp
+++ b/ggml/src/ggml-backend-reg.cpp
@@ -4,7 +4,9 @@
 #include "ggml-impl.h"
 #include <algorithm>
 #include <cstring>
-#include <filesystem>
+#if !defined(__ANDROID__) || __ANDROID_API__ >= 21
+#    include <filesystem>
+#endif
 #include <memory>
 #include <string>
 #include <type_traits>
@@ -86,6 +88,7 @@
 #include "ggml-openvino.h"
 #endif
 
+#if !defined(__ANDROID__) || __ANDROID_API__ >= 21
 namespace fs = std::filesystem;
 
 static std::string path_str(const fs::path & path) {
@@ -102,6 +105,7 @@ static std::string path_str(const fs::path & path) {
         return std::string();
     }
 }
+#endif
 
 struct ggml_backend_reg_entry {
     ggml_backend_reg_t reg;
@@ -198,6 +202,53 @@ struct ggml_backend_registry {
         devices.push_back(device);
     }
 
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+    ggml_backend_reg_t load_backend(const char * path, bool silent) {
+        dl_handle_ptr handle { dl_load_library(path) };
+        if (!handle) {
+            if (!silent) {
+                GGML_LOG_ERROR("%s: failed to load %s: %s\n", __func__, path, dl_error());
+            }
+            return nullptr;
+        }
+
+        auto score_fn = (ggml_backend_score_t) dl_get_sym(handle.get(), "ggml_backend_score");
+        if (score_fn && score_fn() == 0) {
+            if (!silent) {
+                GGML_LOG_INFO("%s: backend %s is not supported on this system\n", __func__, path);
+            }
+            return nullptr;
+        }
+
+        auto backend_init_fn = (ggml_backend_init_t) dl_get_sym(handle.get(), "ggml_backend_init");
+        if (!backend_init_fn) {
+            if (!silent) {
+                GGML_LOG_ERROR("%s: failed to find ggml_backend_init in %s\n", __func__, path);
+            }
+            return nullptr;
+        }
+
+        ggml_backend_reg_t reg = backend_init_fn();
+        if (!reg || reg->api_version != GGML_BACKEND_API_VERSION) {
+            if (!silent) {
+                if (!reg) {
+                    GGML_LOG_ERROR("%s: failed to initialize backend from %s: ggml_backend_init returned NULL\n",
+                        __func__, path);
+                } else {
+                    GGML_LOG_ERROR("%s: failed to initialize backend from %s: incompatible API version (backend: %d, current: %d)\n",
+                        __func__, path, reg->api_version, GGML_BACKEND_API_VERSION);
+                }
+            }
+            return nullptr;
+        }
+
+        GGML_LOG_INFO("%s: loaded %s backend from %s\n", __func__, ggml_backend_reg_name(reg), path);
+
+        register_backend(reg, std::move(handle));
+
+        return reg;
+    }
+#else
     ggml_backend_reg_t load_backend(const fs::path & path, bool silent) {
         dl_handle_ptr handle { dl_load_library(path) };
         if (!handle) {
@@ -243,6 +294,7 @@ struct ggml_backend_registry {
 
         return reg;
     }
+#endif
 
     void unload_backend(ggml_backend_reg_t reg, bool silent) {
         auto it = std::find_if(backends.begin(), backends.end(),
@@ -372,13 +424,29 @@ ggml_backend_t ggml_backend_init_best(void) {
 
 // Dynamic loading
 ggml_backend_reg_t ggml_backend_load(const char * path) {
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
     return get_reg().load_backend(path, false);
+#else
+    return get_reg().load_backend(fs::u8path(path), false);
+#endif
 }
 
 void ggml_backend_unload(ggml_backend_reg_t reg) {
     get_reg().unload_backend(reg, true);
 }
 
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+
+void ggml_backend_load_all() {
+    // Backends are registered statically; no directory scan on API 16.
+}
+
+void ggml_backend_load_all_from_path(const char * dir_path) {
+    (void) dir_path;
+}
+
+#else
+
 static fs::path get_executable_path() {
 #if defined(__APPLE__)
     // get executable path
@@ -572,3 +640,5 @@ void ggml_backend_load_all_from_path(const char * dir_path) {
         ggml_backend_load(backend_path);
     }
 }
+
+#endif // !(__ANDROID__ && __ANDROID_API__ < 21)
diff --git a/ggml/src/ggml-rpc/ggml-rpc.cpp b/ggml/src/ggml-rpc/ggml-rpc.cpp
index 42e489c51..065843c0a 100644
--- a/ggml/src/ggml-rpc/ggml-rpc.cpp
+++ b/ggml/src/ggml-rpc/ggml-rpc.cpp
@@ -28,16 +28,36 @@
 #endif
 #include <cstring>
 #include <fstream>
-#include <filesystem>
 #include <algorithm>
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+#  include <sys/stat.h>
+#  include <unistd.h>
+#else
+#  include <filesystem>
+#endif
 
 static const char * RPC_DEBUG = std::getenv("GGML_RPC_DEBUG");
 
 #define LOG_DBG(...) \
     do { if (RPC_DEBUG) GGML_LOG_DEBUG(__VA_ARGS__); } while (0)
 
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+static std::string rpc_path_join(const char * dir, const char * name) {
+    std::string path(dir ? dir : "");
+    if (!path.empty() && path.back() != '/') {
+        path.push_back('/');
+    }
+    path += name;
+    return path;
+}
 
+static bool rpc_path_exists(const std::string & path) {
+    struct stat st;
+    return stat(path.c_str(), &st) == 0;
+}
+#else
 namespace fs = std::filesystem;
+#endif
 
 static constexpr size_t MAX_CHUNK_SIZE = 1024ull * 1024ull * 1024ull; // 1 GiB
 
@@ -1250,10 +1270,17 @@ bool rpc_server::set_tensor(const std::vector<uint8_t> & input) {
         char hash_str[17];
         snprintf(hash_str, sizeof(hash_str), "%016" PRIx64, hash);
         // save to cache_dir/hash_str
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+        const std::string cache_file = rpc_path_join(cache_dir, hash_str);
+        std::ofstream ofs(cache_file, std::ios::binary);
+        ofs.write((const char *)data, size);
+        GGML_LOG_INFO("[%s] saved to '%s'\n", __func__, cache_file.c_str());
+#else
         fs::path cache_file = fs::path(cache_dir) / hash_str;
         std::ofstream ofs(cache_file, std::ios::binary);
         ofs.write((const char *)data, size);
         GGML_LOG_INFO("[%s] saved to '%s'\n", __func__, cache_file.c_str());
+#endif
     }
     ggml_backend_tensor_set(tensor, data, offset, size);
     return true;
@@ -1266,6 +1293,14 @@ bool rpc_server::get_cached_file(uint64_t hash, std::vector<uint8_t> & data) {
     }
     char hash_str[17];
     snprintf(hash_str, sizeof(hash_str), "%016" PRIx64, hash);
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
+    const std::string cache_file = rpc_path_join(cache_dir, hash_str);
+    if (!rpc_path_exists(cache_file)) {
+        GGML_LOG_INFO("[%s] cache miss for '%s'\n", __func__, cache_file.c_str());
+        return false;
+    }
+    std::ifstream ifs(cache_file, std::ios::binary);
+#else
     fs::path cache_file = fs::path(cache_dir) / hash_str;
     std::error_code ec;
     if (!fs::exists(cache_file, ec)) {
@@ -1273,12 +1308,17 @@ bool rpc_server::get_cached_file(uint64_t hash, std::vector<uint8_t> & data) {
         return false;
     }
     std::ifstream ifs(cache_file, std::ios::binary);
+#endif
     ifs.seekg(0, std::ios::end);
     size_t size = ifs.tellg();
     ifs.seekg(0, std::ios::beg);
     data.resize(size);
     ifs.read((char *)data.data(), size);
+#if defined(__ANDROID__) && __ANDROID_API__ < 21
     GGML_LOG_INFO("[%s] cache hit for '%s' (%zu bytes)\n", __func__, cache_file.c_str(), size);
+#else
+    GGML_LOG_INFO("[%s] cache hit for '%s' (%zu bytes)\n", __func__, cache_file.c_str(), size);
+#endif
     return true;
 }
 
